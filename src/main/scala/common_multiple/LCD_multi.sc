package common_multiple

object LCD_multi {

	def lcd(first: Int, second: Int*): Int = {
	
		def inner(f: Int, s: List[Int]): Int = {
			 
		
		
			1
  		}
  		
  		inner(first, second.toList)
  }                                               //> lcd: (first: Int, second: Int*)Int
  
}