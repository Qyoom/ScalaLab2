package lab.math.calculus.derivative.schaum

object derivative_lab_1 {

  //  y = f(x) is the equation of the curve, i.e. the curve represents the function of x
  // x0 = any value of x
  // y0 = f(x0)
  
  // slope of curve is tangent
  
  /*
  m = lim x -> x0 of (y - y0) / (x - x0)
  can be rewritten as (is equivalent to)
  m = lim x -> x0 of (f(x) - f(x0)) / (x - x0)
  Also see my pencil notes
  */
    
  '''                                             //> res0: Char('\'') = '
}
/*



*/